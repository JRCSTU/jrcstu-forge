{% set name = "pandalone" %}
{% set version = "0.2.7" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 404a8a83bb416b02eb72ff66f67c8b734db6ebb22ae39cbf1d1feb3f036caf8e

build:
  number: 0
  noarch: python
  entry_points:
    - pndl = pandalone.__main__:main
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv"

requirements:
  build:
    - setuptools
  host:
    - asteval >=0.9.7
    - future >=0.15.0
    - jsonschema >=2.5, <3
    - numpy
    - pip
    - python >=3.3
    - toolz
  run:
    - asteval >=0.9.7
    - future >=0.15.0
    - jsonschema >=2.5, <3
    - numpy
    - python
    - toolz

test:
  imports:
    - pandalone
    - pandalone.xleash
    - pandalone.xleash.io
  commands:
    - nosetests
  # source_files:
  #   - tests/*
  requires:
    - coverage
    - coveralls
    - ddt
    - nose
    - pandas
    - xlrd

about:
  home: https://github.com/{{ name }}/{{ name }}
  license: European Union Public Licence 1.1 (EUPL 1.1)
  license_family: OTHER
  license_file: ../../../LICENSE.txt
  summary: Process data-trees with relocatable-paths in Python

  doc_url:  https://{{ name }}.readthedocs.org/
  dev_url:  https://github.com/{{ name }}/{{ name }}

extra:
  recipe-maintainers:
    - ankostis
